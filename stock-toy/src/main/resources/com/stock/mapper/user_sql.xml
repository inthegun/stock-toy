<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">

 	<select id="selectUserById" resultType="com.stock.domain.CustomUserDetails"> <!-- resultType 반드시 있어야함 -->
		<![CDATA[
			SELECT
				*
			FROM
				tbl_user
			WHERE
				ID=#{ loginId}
		]]> <!-- 로그인 페이지에서 지정한 ID의 input 태그의 있는 name 속성을 적어준다  -->
	</select>
	
	
	
 </mapper>